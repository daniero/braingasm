require 'spec_helper'

describe Braingasm do
  it 'has a version number' do
    expect(Braingasm::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
